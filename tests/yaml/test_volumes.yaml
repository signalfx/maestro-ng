__maestro:
  schema: 2

name: test-volumes
ships:
  ship:
    ip: localhost
    docker_version: "1.12"
services:
  service:
    image: ubuntu
    instances:
      instance-1:
        ship: ship
        volumes:
          /out: /in1
        container_volumes:
          - /in2
      instance-2:
        ship: ship
        volumes:
          /out: /in3
        volumes_from: instance-1
